apiVersion: v1
kind: Service
metadata: 
    name: mywebserversvc
spec: 
    type: NodePort
    ports: 
        - port: 80
          protocol: TCP
    selectors: 
        app: httpd # must match with Deployment labels to maintain the connection